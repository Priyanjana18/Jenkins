pipeline{
    agent any
    stages{
        stage('checkout') {
            steps{
                git branch: 'dev',
                    url: 'https://github.com/Priyanjana18/Jenkins.git'
                sh "ls -lat"
            }
        }
        stage('print') {
            steps{
                echo "Hello I am in pipeline"
            }
        }
    }
}
